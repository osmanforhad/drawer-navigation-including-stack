import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textStle: {
        fontSize: 30,
        color: 'black',
        textAlign: 'center',
        marginTop: 10,
    },
    buttonStyle: {
        width: '100%',
        borderRadius: 3,
        marginBottom: 10,
    },
});

export default styles;